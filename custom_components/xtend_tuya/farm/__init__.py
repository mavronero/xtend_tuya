"""Farm domain (L3): runs, locations, calendar, dashboard cards.

Knows valves only through the HA contract in `contract.py` (entities,
translation keys, attributes, services), never through Tuya internals.
See docs/architecture.md.
"""
